import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const routes=['product','solutions','cases','resources','pricing','diagnosis','about','design-system','pages','login','thank-you','terms','privacy'];let m=[];for(const r of routes)if(!fs.existsSync(path.join(root,r,'index.html')))m.push(r);if(m.length){console.error('missing clean route directories: '+m.join(', '));process.exit(1);}console.log('OK: clean URL route directories exist.');
