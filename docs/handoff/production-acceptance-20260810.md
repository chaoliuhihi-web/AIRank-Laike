# AIRank 生产验收记录（2026-08-10）

## 结论

- 在线客户体验已部署到 `https://console.airank.net.cn`，公网 Web、API、MySQL、MinIO、Scheduler 和 3 个 Worker 均可用。
- Intel AI PC 真实盲测基线完成 45/45 条有效样本；36 条正常未提及保留并计入分母，Provider 原生引用为 0。
- 客户六步闭环已走到不可变发布包；由于尚未登记真实外部发布 URL 与截图，T+7/T+14/T+30 复测窗口没有伪造启动。
- 当前状态是“可在线体验、可继续受控交付”，不是商业生产放行。生产 `release` preflight 仍因暴露凭证未完成轮换而阻断。

## 本轮发现并修复

| 问题 | 影响 | 修复 | 证据 |
| --- | --- | --- | --- |
| API 容器重建后 Web 仍缓存旧容器地址，出现 502 | 公网不可用 | Nginx 使用 Docker DNS 动态解析 API upstream | API 重建后 Web 不重建，公网 `/api/v1/ready` 仍成功 |
| Provider 搜索配置与工具回退混为一个字段 | 相同测量计划被误拆成不同质量分组 | 分离 `search_configured`、`search_requested`、`search_used` 和 fallback 结果 | Intel 第二轮质量报告通过，仍保留豆包工具回退事实 |
| 新建答案资产后 URL 已跳转，但页面仍显示新建表单 | 客户可能误判失败并重复提交 | 创建成功后把返回资产写入当前页面状态 | 前端构建与分包门禁通过；重复测试草稿已在审核中驳回 |
| 客户资产审核页只有“查看”，没有批准、驳回和要求修改动作 | 六步闭环在审核节点中断 | 补齐人工审核动作、状态更新与错误反馈 | 1 个资产通过、1 个重复草稿驳回；通过项进入发布页 |
| “查看支持样本”只回到扫描总览 | 指标和缺口无法下钻到真实样本 | 新增客户样本详情页，展示问题、独立采样、原始回答、Provider request ID、证据等级、快照与哈希 | Chrome 与应用内浏览器均完成 45 条列表及单样本下钻 |
| DeepSeek 卡片仍显示旧默认模型 | Provider 状态表达失真 | 运行时明确使用 `deepseek-v3.2` 并保持停用 | 线上显示 `deepseek-v3.2 / Provider 已停用` |

## Intel AI PC 真实证据

- 项目：`project_1ff9e2af2cbd`
- 基线：`scan_run_1d3070d4dcd6`
- 5 个已确认盲测问题 × 3 个 Provider × 3 次独立采样 = 45 个任务。
- 45 成功、0 失败；4 次 Provider 并发事件按 `deferred` 留痕后重试成功。
- 有效样本率 100%；品牌提及率 20%；明确推荐率 2.2222%；Top1/3/5 均为 0；稳定性 0.844444。
- 36 条正常未提及；0 条 Provider 原生引用。
- 质量报告 SHA-256：`95211d077e577e185a3b29d07b349b9891e6b6da2bac2412935e96f717ae08a9`。
- 9 个证据缺口；1 条官方来源事实已审核；1 个证据绑定 FAQ 已审核；1 个不可变发布包已生成。

## 工程与运行门禁

- Python：`691 passed, 40 skipped`。
- Web：TypeScript + Vite 构建通过；4 个 JS chunk，单 chunk 上限 512000 bytes 通过。
- 公网 `/healthz` 正常；`/api/v1/ready` 的生产配置、数据库、schema `20260809_0047`、对象存储均 ready。
- 主要客户页面真实数据就绪约 0.05–1.12 秒；发布页约 0.59 秒。
- 服务器：7.1 GiB 内存，验收时约 3.2 GiB available；根盘约 65 GB 可用；数据盘约 37 GB 可用；Swap 未使用。
- 数据盘备份：`mysql-post-e4a9c9e-20260809T163602Z.sql.gz`，968595 bytes，gzip 校验通过。

## 未解除门禁

1. 旧 Kimi 凭证曾出现在会话记录；上线前必须轮换，并设置 `AIRANK_COMPROMISED_CREDENTIALS_ROTATED=true` 的可审计证明。密钥不得写入代码、Git、日志或报告。
2. DeepSeek v4-pro 当前账户无额度；v3.2 虽可调用但已保持停用，必须在开通新模型额度并通过 L3 探测后再启用。
3. 本基线没有 Provider 原生引用，因此引用召回率和引用支持度不可宣称已验证。
4. 事实准确率尚无完整双人复核样本，不得输出商业准确率结论。
5. 尚无真实外部发布 URL、发布截图和抓取验证；T+7/T+14/T+30 只能在真实发布后按时间推进。
6. 四平台真实重复采样门禁尚未满足：当前稳定可用的是豆包、千问、Kimi 三个平台，DeepSeek 停用。

满足以上门禁前，不使用“商业可用”“正式上线通过”或“发布后一定被推荐”等口径。
