# CodexWin Packet Status

| Packet | Status | Commit | Notes |
| --- | --- | --- | --- |
| M1-WIN-001 | done | e2e240b | Health/version envelope completed and contract tests passed. |
| M1-WIN-001B | review | this commit | Unified API error envelope now uses the contract error registry and preserves `trace_id` for HTTP/validation/unexpected errors. |
| M1-WIN-001C | review | this commit | Project, competitor, and buyer question request/response JSON Schemas are frozen without DB persistence. |
| M1-WIN-001D | dev_only | this commit | Project, competitor, and buyer question APIs are wired to an in-memory repository adapter; not production persistence. |
| M1-WIN-002 | review | this commit | MySQL repository path is implemented behind `AIRANK_DATABASE_URL`; CodexMacPro verified real local MySQL migration and project/competitor/question writes on 2026-05-17. |
| M2-WIN-000 | review | this commit | Scan run create/status and scan task status JSON Schemas are frozen without worker scheduling. |
| M2-WIN-001 | review | this commit | Scan run/task APIs now use a MySQL repository path behind `AIRANK_DATABASE_URL`, persist scan tasks, and enqueue `airank_async_jobs`; CodexMacPro verified the real local MySQL path on 2026-05-17. |
| M3-WIN-001 | review | this commit | Fact review API supports confirmed/rejected/needs_redaction/private and rejects confirmed reviews without traceable source refs. |
| M3-WIN-002 | review | this commit | AI asset bundle API uses a MySQL repository path behind `AIRANK_DATABASE_URL` to derive assets from content assets, gaps, and publish packages; CodexMacPro verified the real local MySQL path on 2026-05-17. |
| M4-WIN-001 | review | this commit | Reports API uses a MySQL repository path behind `AIRANK_DATABASE_URL` to read `airank_reports` and write download receipts into `airank_audit_events`; CodexMacPro verified the real local MySQL path on 2026-05-17. |
| M4-WIN-002 | review | this commit | Console overview keeps API-first loading and shows an explicit fallback banner when API data is unavailable. |
| AUTH-WIN-LOGIN-BRIDGE | review | this commit | Added `/api/v1/auth/login` contract/API bridge, yudao permission-info verification, explicit `dev_only` login adapter, and Web session-driven `Authorization`/`tenant-id` headers. |
| WEB-WIN-FLOW-HARDENING | review | this commit | Console buttons now have deterministic navigation, toast, detail panel, form, or API-backed receipt behavior; mobile layout tightened for 390px QA. |
| RELEASE-WIN-SCHEMA-UX-HARDENING | review | this commit | Added forward Alembic reconciliation for URL column lengths on existing MySQL deployments and removed the login-time fallback banner flash before real overview data loads. |

## Run Log

- 2026-05-17: `M1-WIN-001` completed by CodexWin.
- 2026-05-17: `M1-WIN-001B` implemented in a clean CodexWin worktree because the original Windows main worktree is diverged and contains local website edits that must not be reset/stashed. Scope: `apps/api`, `packages/contracts`, `tests/contracts`. Validation: `python -m pytest tests/contracts -q` passed 6 tests; `git diff --check` passed; `cd apps/web && npm run build` passed after `npm ci` restored local ignored `node_modules`; `PYTHONUTF8=1 python scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M1-WIN-001C` froze contract skeletons for low-touch project creation, Hermes-discovered competitors, and Hermes-generated buyer questions. Scope: `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python -m pytest tests/contracts -q` passed 12 tests; `git diff --check` passed; `cd apps/web && npm run build` passed; `PYTHONUTF8=1 python scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M1-WIN-001D` added dev-only in-memory repository wiring for `POST /api/v1/projects`, `POST /api/v1/projects/{project_id}/competitors`, and `POST /api/v1/projects/{project_id}/buyer-questions`. Scope: `apps/api`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python3 -m pytest tests/contracts -q` passed 14 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M1-WIN-002` added SQLAlchemy/MySQL repository writes for projects, competitors, and buyer questions when `AIRANK_DATABASE_URL` is configured; default no-env mode remains in-memory for local contract tests. Validation: `python3 -m pytest tests/contracts -q` passed 15 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed. Real validation command `cd apps/api && AIRANK_DATABASE_URL=mysql+pymysql://airank:airank_dev_password@127.0.0.1:3306/airank_laike?charset=utf8mb4 python3 -m alembic upgrade head` is environment-blocked by MySQL access denied `(1045)` for user `airank`.
- 2026-05-17: `M2-WIN-000` added schema-only contracts for scan run creation/status and scan task status. Scope: `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`; no worker scheduling or API behavior implemented. Validation: `python3 -m pytest tests/contracts -q` passed 22 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M2-WIN-001` added dev-only scan run APIs: create run, get run status, list run tasks, and get task status. Scope: `apps/api`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`; no worker scheduling implemented. Validation: `python3 -m pytest tests/contracts -q` passed 25 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M2-WIN-001` production hardening added `MySQLScanRepository`. With `AIRANK_DATABASE_URL`, scan run creation now verifies tenant/project/question rows, persists `airank_scan_runs` and `airank_scan_tasks`, and enqueues one `airank_async_jobs` row per provider/question task with provider/question payload for worker consumption. Default no-env mode remains in-memory for local contract tests. Validation: `python3 -m pytest tests/contracts/test_scan_run_api_contract.py -q` passed 9 tests; `python3 -m pytest tests/contracts -q` passed 48 tests; `python3 -m pytest tests/acceptance -q` passed 9 tests; `python3 -m py_compile apps/api/main.py` passed; `git diff --check` passed; `cd apps/web && npm run build` passed; `python3 scripts/agent_control.py gate --write` passed; worker 7, score 3, evidence 9, xinghe-adapter 2 package tests passed. Initial real DB validation was environment-blocked at that time and is superseded by the CodexMacPro fresh-DB verification below.
- 2026-05-17: `M3-WIN-001` added fact review request/response contracts and `PATCH /api/v1/projects/{project_id}/facts/{fact_id}/review`. Confirmed review requires traceable source refs; rejected, needs_redaction, and private map to explicit fact status/disclosure. Validation: `python3 -m pytest tests/contracts -q` passed 31 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M3-WIN-002` added `GET /api/v1/projects/{project_id}/asset-bundle`, `asset_bundle_response.schema.json`, and changed the AI 收录包 page to read API data with existing fallback. Scope: `apps/api`, `apps/web`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python3 -m pytest tests/contracts -q` passed 32 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M3-WIN-002` production hardening added `MySQLAssetBundleRepository`. With `AIRANK_DATABASE_URL`, the asset bundle API verifies tenant/project scope and derives payloads from `airank_content_assets`, `airank_content_gaps`, and `airank_publish_packages`; no-env mode remains a local web fallback. Validation: `python3 -m pytest tests/contracts/test_asset_bundle_api_contract.py -q` passed 6 tests; `python3 -m pytest tests/contracts -q` passed 52 tests; `python3 -m pytest tests/acceptance -q` passed 9 tests; `python3 -m py_compile apps/api/main.py` passed; `git diff --check` passed; `cd apps/web && npm run build` passed; `python3 scripts/agent_control.py gate --write` passed. Initial real DB validation was environment-blocked at that time and is superseded by the CodexMacPro fresh-DB verification below.
- 2026-05-17: `M4-WIN-001` added report list and download receipt APIs, contracts, and web report page API loading with fallback. Scope: `apps/api`, `apps/web`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python3 -m pytest tests/contracts -q` passed 33 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M4-WIN-001` production hardening added `MySQLReportRepository`. With `AIRANK_DATABASE_URL`, report listing verifies tenant/project scope and reads `airank_reports`; download receipt creation verifies the tenant-owned report and writes a `report.download_receipt` event into `airank_audit_events`. No-env mode remains a local web fallback. Validation: `python3 -m pytest tests/contracts/test_report_api_contract.py -q` passed 6 tests; `python3 -m pytest tests/contracts -q` passed 56 tests; `python3 -m pytest tests/acceptance -q` passed 9 tests; `python3 -m py_compile apps/api/main.py` passed; `git diff --check` passed; `cd apps/web && npm run build` passed; `python3 scripts/agent_control.py gate --write` passed. Initial real DB validation was environment-blocked at that time and is superseded by the CodexMacPro fresh-DB verification below.
- 2026-05-17: `M4-WIN-002` added explicit console overview API source state. The dashboard uses API data when available and shows a visible fallback banner when it falls back to local data. Validation: `cd apps/web && npm run build` passed; `python3 -m pytest tests/contracts -q` passed 33 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: CodexMacPro release verification cleared the previous `M1-WIN-002` environment blocker: `airank` connected to local Docker MySQL, real Alembic `upgrade head` passed, and FastAPI TestClient with `AIRANK_DATABASE_URL` created project, competitor, and buyer question rows through the MySQL repository.
- 2026-05-17: CodexMacPro release verification also cleared the `M2-WIN-001` MySQL environment blocker on a fresh `airank_laike_release_gate` database: project and buyer question API writes passed, scan run creation returned `201`, scan task list returned 2 tasks, and `airank_async_jobs` contained 2 queued provider jobs.
- 2026-05-17: CodexMacPro release verification cleared the `M3-WIN-002` and `M4-WIN-001` MySQL environment blockers on a fresh `airank_laike_release_gate` database: asset bundle returned 1 DB-derived asset, report list returned 1 DB report, download receipt returned `201`, and `airank_audit_events` contained 1 `report.download_receipt` event.
- 2026-05-17: `AUTH-WIN-LOGIN-BRIDGE` closed the product login gap found during local上线 simulation. Scope: `apps/api`, `apps/web`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Added login request/response schemas, registered `AUTH_LOGIN_FAILED`, bridged yudao login plus permission-info validation, added an explicit `AIRANK_AUTH_MODE=dev_only` adapter for local demos, and changed Web API calls to use the stored session token and tenant instead of hardcoded headers. Validation: `python -X utf8 -m pytest tests/contracts/test_auth_login_contract.py -q` passed 3 tests; `python -X utf8 -m pytest tests/contracts -q` passed 59 tests; `python scripts/agent_control.py gate --write` passed with expected working-tree changes; `git diff --check` passed; `cd apps/web && npm run build` passed; Browser smoke passed `/login` -> dev-only login -> `/console` -> `/console/assets` -> logout back to `/login` with no console warnings/errors.
- 2026-05-17: Follow-up on `AUTH-WIN-LOGIN-BRIDGE`: full release readiness on clean `1094b87` caught one contract test isolation issue when the release shell exported `YUDAO_PERMISSION_INFO_URL`; the yudao bridge test now deletes explicit yudao URL env overrides before asserting `YUDAO_BASE_URL` URL construction. This is a test-only hardening patch before rerunning release readiness.
- 2026-05-17: Full release readiness passed on clean `c2fa9ce` after the auth-login test isolation fix. Result: working tree PASS, GitHub/Gitee main refs PASS, diff check PASS, tracked runtime artifact check PASS, contracts 59 PASS, acceptance 15 PASS, worker 11 PASS, score 3 PASS, evidence 9 PASS, xinghe-adapter 5 PASS, Web build PASS, real integration 6 PASS, Alembic offline SQL PASS, Alembic real MySQL PASS, and MVP capability probe PASS with yudao auth, yudao tenant/user, and filesystem object storage ready. Optional Xinghe/Hermes services remain `dev_only` warnings unless configured by deployment.
- 2026-05-17: `WEB-WIN-FLOW-HARDENING` closed the console interaction gaps found during senior release QA. Scope: `apps/web`, `docs/handoff/status/codex-win.md`. Added shared toast/detail-panel feedback, made help/date/facts/questions/assets/publishing/assistant/reports/settings controls deterministic, surfaced report download receipt API failures, and tightened mobile layout/table containment for 390px viewports. Validation: Browser QA passed `/login` -> dev-only login plus all console routes, 28 desktop button/form/navigation checks, report receipt API click, settings save, logout, 390x844 mobile route headings, and zero browser warning/error logs. `python scripts/agent_control.py gate --write` passed with expected working-tree changes; `python -m pytest tests/contracts -q` passed 59 tests; `git diff --check` passed; `cd apps/web && npm run build` passed.
- 2026-05-17: `RELEASE-WIN-SCHEMA-UX-HARDENING` fixed two launch blockers found during final senior QA. Scope: `apps/api/alembic`, `apps/web`, `tests/contracts`, `tests/integration`, `docs/handoff/status/codex-win.md`. Added `20260517_0002` to reconcile existing MySQL deployments whose URL columns were already stamped at the old Alembic head, updated real integration tests to expect the new head and all URL-like columns at 2048, and changed login state handling so the console does not show a transient fallback banner before real overview data returns. Validation: `python -m pytest tests/contracts -q` passed 59 tests; `python -m pytest tests/acceptance -q` passed 15 tests; real MySQL `AIRANK_RUN_REAL_MYSQL=1 ... pytest tests/integration -q` passed 5 and skipped yudao-only 1; `python -X utf8 -m alembic upgrade head --sql` passed; `cd apps/web && npm run build` passed; `git diff --check` passed; `python scripts/agent_control.py gate --write` passed; release UI QA with system Chrome passed login, 11 routes, 10 sidebar nav buttons, 36/36 main page buttons, assistant typed question, checkbox toggle, report download receipt, 390x844 mobile no-overflow checks, logout, and zero API/console/page errors.
