# CodexWin Packet Status

| Packet | Status | Commit | Notes |
| --- | --- | --- | --- |
| M1-WIN-001 | done | e2e240b | Health/version envelope completed and contract tests passed. |
| M1-WIN-001B | review | this commit | Unified API error envelope now uses the contract error registry and preserves `trace_id` for HTTP/validation/unexpected errors. |
| M1-WIN-001C | review | this commit | Project, competitor, and buyer question request/response JSON Schemas are frozen without DB persistence. |
| M1-WIN-001D | dev_only | this commit | Project, competitor, and buyer question APIs are wired to an in-memory repository adapter; not production persistence. |
| M1-WIN-002 | review_env_blocked | this commit | MySQL repository path is implemented behind `AIRANK_DATABASE_URL`, but real local MySQL validation is blocked by access denied for the dev `airank` user. |
| M2-WIN-000 | review | this commit | Scan run create/status and scan task status JSON Schemas are frozen without worker scheduling. |
| M2-WIN-001 | review_env_blocked | this commit | Scan run/task APIs now use a MySQL repository path behind `AIRANK_DATABASE_URL`, persist scan tasks, and enqueue `airank_async_jobs`; real local MySQL validation is still blocked by access denied for the dev `airank` user. |
| M3-WIN-001 | review | this commit | Fact review API supports confirmed/rejected/needs_redaction/private and rejects confirmed reviews without traceable source refs. |
| M3-WIN-002 | review_env_blocked | this commit | AI asset bundle API now uses a MySQL repository path behind `AIRANK_DATABASE_URL` to derive assets from content assets, gaps, and publish packages; real local MySQL validation is still blocked by access denied for the dev `airank` user. |
| M4-WIN-001 | dev_only | this commit | Reports API and download receipt endpoint are wired; web reports page reads API data with fallback. |
| M4-WIN-002 | review | this commit | Console overview keeps API-first loading and shows an explicit fallback banner when API data is unavailable. |

## Run Log

- 2026-05-17: `M1-WIN-001` completed by CodexWin.
- 2026-05-17: `M1-WIN-001B` implemented in a clean CodexWin worktree because the original Windows main worktree is diverged and contains local website edits that must not be reset/stashed. Scope: `apps/api`, `packages/contracts`, `tests/contracts`. Validation: `python -m pytest tests/contracts -q` passed 6 tests; `git diff --check` passed; `cd apps/web && npm run build` passed after `npm ci` restored local ignored `node_modules`; `PYTHONUTF8=1 python scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M1-WIN-001C` froze contract skeletons for low-touch project creation, Hermes-discovered competitors, and Hermes-generated buyer questions. Scope: `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python -m pytest tests/contracts -q` passed 12 tests; `git diff --check` passed; `cd apps/web && npm run build` passed; `PYTHONUTF8=1 python scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M1-WIN-001D` added dev-only in-memory repository wiring for `POST /api/v1/projects`, `POST /api/v1/projects/{project_id}/competitors`, and `POST /api/v1/projects/{project_id}/buyer-questions`. Scope: `apps/api`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python3 -m pytest tests/contracts -q` passed 14 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M1-WIN-002` added SQLAlchemy/MySQL repository writes for projects, competitors, and buyer questions when `AIRANK_DATABASE_URL` is configured; default no-env mode remains in-memory for local contract tests. Validation: `python3 -m pytest tests/contracts -q` passed 15 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed. Real validation command `cd apps/api && AIRANK_DATABASE_URL=mysql+pymysql://airank:airank_dev_password@127.0.0.1:3306/airank_laike?charset=utf8mb4 python3 -m alembic upgrade head` is environment-blocked by MySQL access denied `(1045)` for user `airank`.
- 2026-05-17: `M2-WIN-000` added schema-only contracts for scan run creation/status and scan task status. Scope: `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`; no worker scheduling or API behavior implemented. Validation: `python3 -m pytest tests/contracts -q` passed 22 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M2-WIN-001` added dev-only scan run APIs: create run, get run status, list run tasks, and get task status. Scope: `apps/api`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`; no worker scheduling implemented. Validation: `python3 -m pytest tests/contracts -q` passed 25 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M2-WIN-001` production hardening added `MySQLScanRepository`. With `AIRANK_DATABASE_URL`, scan run creation now verifies tenant/project/question rows, persists `airank_scan_runs` and `airank_scan_tasks`, and enqueues one `airank_async_jobs` row per provider/question task with provider/question payload for worker consumption. Default no-env mode remains in-memory for local contract tests. Validation: `python3 -m pytest tests/contracts/test_scan_run_api_contract.py -q` passed 9 tests; `python3 -m pytest tests/contracts -q` passed 48 tests; `python3 -m pytest tests/acceptance -q` passed 9 tests; `python3 -m py_compile apps/api/main.py` passed; `git diff --check` passed; `cd apps/web && npm run build` passed; `python3 scripts/agent_control.py gate --write` passed; worker 7, score 3, evidence 9, xinghe-adapter 2 package tests passed. Real validation command `cd apps/api && AIRANK_DATABASE_URL=mysql+pymysql://airank:airank_dev_password@127.0.0.1:3306/airank_laike?charset=utf8mb4 python3 -m alembic upgrade head` remains environment-blocked by MySQL access denied `(1045)` for user `airank`.
- 2026-05-17: `M3-WIN-001` added fact review request/response contracts and `PATCH /api/v1/projects/{project_id}/facts/{fact_id}/review`. Confirmed review requires traceable source refs; rejected, needs_redaction, and private map to explicit fact status/disclosure. Validation: `python3 -m pytest tests/contracts -q` passed 31 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M3-WIN-002` added `GET /api/v1/projects/{project_id}/asset-bundle`, `asset_bundle_response.schema.json`, and changed the AI 收录包 page to read API data with existing fallback. Scope: `apps/api`, `apps/web`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python3 -m pytest tests/contracts -q` passed 32 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M3-WIN-002` production hardening added `MySQLAssetBundleRepository`. With `AIRANK_DATABASE_URL`, the asset bundle API verifies tenant/project scope and derives payloads from `airank_content_assets`, `airank_content_gaps`, and `airank_publish_packages`; no-env mode remains a local web fallback. Validation: `python3 -m pytest tests/contracts/test_asset_bundle_api_contract.py -q` passed 6 tests; `python3 -m pytest tests/contracts -q` passed 52 tests; `python3 -m pytest tests/acceptance -q` passed 9 tests; `python3 -m py_compile apps/api/main.py` passed; `git diff --check` passed; `cd apps/web && npm run build` passed; `python3 scripts/agent_control.py gate --write` passed. Real local MySQL validation remains blocked by access denied `(1045)` for user `airank`.
- 2026-05-17: `M4-WIN-001` added report list and download receipt APIs, contracts, and web report page API loading with fallback. Scope: `apps/api`, `apps/web`, `packages/contracts`, `tests/contracts`, `docs/handoff/status/codex-win.md`. Validation: `python3 -m pytest tests/contracts -q` passed 33 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `cd apps/web && npm run build` passed; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
- 2026-05-17: `M4-WIN-002` added explicit console overview API source state. The dashboard uses API data when available and shows a visible fallback banner when it falls back to local data. Validation: `cd apps/web && npm run build` passed; `python3 -m pytest tests/contracts -q` passed 33 tests; `python3 -m pytest tests/acceptance -q` passed 7 tests; `git diff --check` passed; `python3 scripts/agent_control.py gate --write` passed.
